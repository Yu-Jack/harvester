---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    {}
  name: healthsummaries.harvesterhci.io
spec:
  group: harvesterhci.io
  names:
    kind: HealthSummary
    listKind: HealthSummaryList
    plural: healthsummaries
    singular: healthsummary
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          HealthSummary aggregates all ComponentHealth resources into a single cluster-wide overview.
          There is exactly one HealthSummary resource, named "cluster".
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            properties:
              components:
                additionalProperties:
                  properties:
                    errorCount:
                      type: integer
                    warningCount:
                      type: integer
                  required:
                  - errorCount
                  - warningCount
                  type: object
                type: object
              lastCheckedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
